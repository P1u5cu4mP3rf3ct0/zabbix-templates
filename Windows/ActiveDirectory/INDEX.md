# Template placeholder for Windows ActiveDirectory Event Log

This file is an index/placeholder pointing to the original template located at:

windows/ad-domain-controller/template_windows_ad_event_log.yaml

If you want a full copy of the YAML renamed to a search-friendly filename and with internal template names updated from "Windows AD Event Log DC" to "Windows ActiveDirectory Event Log", reply and I will copy and update the YAML contents accordingly.

Suggested filenames for a full renamed template:
- template_windows_active_directory_event_log.yaml
- template_windows_ad_event_log_active_directory.yaml

Suggested template internal names:
- template: Windows ActiveDirectory Event Log
- name: Windows Active Directory Event Log - Domain Controller (2022)

Search tags to include inside YAML tags section where applicable: ActiveDirectory, AD, DomainController, Windows, Zabbix, EventLog, Security, AD-DS
